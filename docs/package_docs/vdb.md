## Historical Package vdb

`magic.vdb` is not part of the current public API.

Use [`rag.vdb`](./rag.vdb.md) instead:

```cangjie
import magic.rag.vdb.*
```

The current vector-store implementation lives in `src/rag/vdb/`.
