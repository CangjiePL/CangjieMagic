## Historical Package storage.graph

`magic.storage.graph` is not part of the current public API.

There is no direct replacement package with the same name in the current source tree.
If you are looking for the supported retrieval stack, use [`core.rag`](./core.rag.md) and [`rag.vdb`](./rag.vdb.md).
